@./AGENTS.md

# Gemini CLI adapter

- Reusable commands are defined under `.gemini/commands/`.
- Canonical rules, workflows and skill instructions live under `.agents/`.
- Use `/memory show` to confirm the project context before work.
