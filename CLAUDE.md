@AGENTS.md

# Claude Code adapter

- Project skills are available under `.claude/skills/` and route to the canonical definitions in `.agents/skills/`.
- Path-specific rules are under `.claude/rules/`.
- Use `/context` to confirm this file was loaded before project work.
